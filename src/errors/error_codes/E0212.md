The parser encountered a token it did not expect at this position.

```iris
let x = )
```

```
error[E0212]: unexpected token ')'
 --> main.iris:1:9
  |
1 | let x = )
  |         ^ unexpected here
= hint: expected a value: number, string, true, false, or identifier
```

Every expression must start with a value - a number, string, `true`,
`false`, an identifier, or `(` for a grouped expression. Check for
mismatched brackets or extra punctuation.